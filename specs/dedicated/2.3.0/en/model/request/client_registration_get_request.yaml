type: object
required: [ clientId, token ]
properties:
  clientId:
    type: string
    description: |
      Client ID.
  token:
    type: string
    description: |
      Client registration access token.
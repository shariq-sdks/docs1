type: object
required: [ json ]
properties:
  clientId:
    type: string
    required: true
    description: |
      Client ID.
  token:
    type: string
    required: true
    description: |
      Client registration access token.
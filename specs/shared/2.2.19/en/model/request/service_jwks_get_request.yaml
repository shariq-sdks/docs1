- in: query
  name: includePrivateKeys
  schema:
    type: boolean
  description: If true, the endpoint returns only the user metadata.
- in: query
  name: pretty
  schema:
    type: boolean
  description: If true, the endpoint returns only the user metadata.
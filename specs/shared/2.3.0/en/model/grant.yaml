type: object
properties:
  scopes:
    type: array
    items:
      $ref: ./grant_scope.yaml
  claims:
    type: array
    items:
      type: string
    description: |
      The claims associated with the Grant.
  authorizationDetails:
    $ref: ./authz_details.yaml

type: object
properties:
  resultCode:
    type: string
    description: The code which represents the result of the API call.
  resultMessage:
    type: string
    description: A short message which explains the result of the API call.
  action:
    $ref: ../en_test.yaml#/components/schemas/VciOfferInfoApiResponse200JsonAction
  info:
    $ref: ../en_test.yaml#/components/schemas/VciOfferInfoApiResponse200JsonInfo

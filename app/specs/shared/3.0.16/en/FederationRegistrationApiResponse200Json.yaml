type: object
properties:
  resultCode:
    type: string
    description: The code which represents the result of the API call.
  resultMessage:
    type: string
    description: A short message which explains the result of the API call.
  action:
    $ref: ../en_test.yaml#/components/schemas/FederationRegistrationApiResponse200JsonAction
  responseContent:
    type: string
    description: >
      The content that the authorization server implementation can use as the
      value of `WWW-Authenticate`

      header on errors.
  client:
    $ref: ../en_test.yaml#/components/schemas/FederationRegistrationApiResponse200JsonClient

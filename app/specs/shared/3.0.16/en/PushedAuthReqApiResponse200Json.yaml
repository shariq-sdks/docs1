type: object
properties:
  resultCode:
    type: string
    description: The code which represents the result of the API call.
  resultMessage:
    type: string
    description: A short message which explains the result of the API call.
  action:
    $ref: ../en_test.yaml#/components/schemas/PushedAuthReqApiResponse200JsonAction
  requestUri:
    type: string
    description: >
      The request_uri created to the client to be used as request_uri on the
      authorize call.
  responseContent:
    type: string
    description: >
      The content that the authorization server implementation is to return to
      the client

      application.
  clientAuthMethod:
    $ref: ../en_test.yaml#/components/schemas/PushedAuthReqApiResponse200JsonClientAuthMethod
  dpopNonce:
    type: string
    description: |
      Get the expected nonce value for DPoP proof JWT, which should be used
      as the value of the `DPoP-Nonce` HTTP header.

type: object
properties:
  resultCode:
    type: string
    description: The code which represents the result of the API call.
  resultMessage:
    type: string
    description: A short message which explains the result of the API call.
  action:
    $ref: ../en_test.yaml#/components/schemas/VciBatchParseApiResponse200JsonAction
  responseContent:
    type: string
    description: The content of the response to the request sender.
  info:
    type: array
    items:
      $ref: ../en_test.yaml#/components/schemas/VciBatchParseApiResponse200JsonInfoItems
    description: |
      Information about the credential requests in the batch credential
      request.

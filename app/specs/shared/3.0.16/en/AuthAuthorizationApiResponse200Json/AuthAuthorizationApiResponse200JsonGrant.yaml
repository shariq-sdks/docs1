type: object
properties:
  scopes:
    type: array
    items:
      $ref: ../../en_test.yaml#/components/schemas/AuthAuthorizationApiResponse200JsonGrantScopesItems
  claims:
    type: array
    items:
      type: string
    description: |
      The claims associated with the Grant.
  authorizationDetails:
    $ref: ../../en_test.yaml#/components/schemas/AuthAuthorizationApiResponse200JsonGrantAuthorizationDetails

type: object
properties:
  resultCode:
    type: string
    description: The code which represents the result of the API call.
  resultMessage:
    type: string
    description: A short message which explains the result of the API call.
  action:
    $ref: ../en_test.yaml#/components/schemas/AuthUserinfoIssueApiResponse200JsonAction
  responseContent:
    type: string
    description: >
      The content that the authorization server implementation can use as the
      value of `WWW-Authenticate`

      header on errors.
  signature:
    type: string
    description: |
      The signature header of the response message.
  signatureInput:
    type: string
    description: |
      The signature-input header of the response message
  contentDigest:
    type: string
    description: |
      The content-digest header of the response message

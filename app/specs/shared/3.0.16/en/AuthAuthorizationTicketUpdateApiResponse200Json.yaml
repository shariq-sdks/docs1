type: object
properties:
  info:
    type: string
    description: Information about the ticket.
  action:
    $ref: ../en_test.yaml#/components/schemas/AuthAuthorizationTicketUpdateApiResponse200JsonAction
  resultCode:
    type: string
    description: The code which represents the result of the API call.
  resultMessage:
    type: string
    description: A short message which explains the result of the API call.

type: object
properties:
  resultCode:
    type: string
    description: The code which represents the result of the API call.
  resultMessage:
    type: string
    description: A short message which explains the result of the API call.
  action:
    $ref: ../en_test.yaml#/components/schemas/HskGetListApiResponse200JsonAction
  hsks:
    type: array
    items:
      $ref: ../en_test.yaml#/components/schemas/HskGetListApiResponse200JsonHsksItems
    description: List of HSK

type: string
enum:
  - OK
  - ACCEPTED
  - UNAUTHORIZED
  - FORBIDDEN
  - INTERNAL_SERVER_ERROR
  - CALLER_ERROR
description: |
  The next action that the implementation of the credential endpoint
  should take.
